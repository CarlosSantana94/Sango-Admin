import { AvisoDePrivacidadPipe } from './aviso-de-privacidad.pipe';

describe('AvisoDePrivacidadPipe', () => {
  it('create an instance', () => {
    const pipe = new AvisoDePrivacidadPipe();
    expect(pipe).toBeTruthy();
  });
});
